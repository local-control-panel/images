{
    # Global options
    admin off
    persist_config off

    servers {
        max_request_body_size 128MB
    }

    frankenphp {
        worker /var/www/html/index.php {$FRANKENPHP_WORKER_PROCESSES:4}
    }
}

# Import per-site Caddyfiles managed by the control panel
import /etc/frankenphp/Caddyfile.d/*.caddyfile
